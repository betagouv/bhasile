model Activite {
  id                       Int      @id @default(autoincrement())
  dnaCode                  String?
  dna                      Dna?     @relation(fields: [dnaCode], references: [code], onDelete: Cascade, onUpdate: Cascade)
  date                     DateTime
  placesAutorisees         Int?
  desinsectisation         Int?
  remiseEnEtat             Int?
  sousOccupation           Int?
  travaux                  Int?
  totalPlacesIndisponibles Int?
  tauxIndisponibilite      Decimal?
  tauxOccupation           Decimal?
  placesOccupees           Int?
  tauxBPI                  Decimal?
  presencesInduesBPI       Int?
  tauxDeboutes             Decimal?
  presencesInduesDeboutees Int?

  @@unique([dnaCode, date])
  @@schema("public")
}
