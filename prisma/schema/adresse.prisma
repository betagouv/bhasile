model Adresse {
  id                 Int                @id @default(autoincrement())
  structureVersion   StructureVersion?  @relation(fields: [structureVersionId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  structureVersionId Int?
  adresse            String?
  codePostal         String?
  commune            String?
  repartition        Repartition?
  placesAutorisees   Int?
  isQpv              Boolean            @default(false)
  isLogementSocial   Boolean            @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@index([structureVersionId])
  @@schema("public")
}

enum Repartition {
  DIFFUS
  COLLECTIF
  MIXTE

  @@schema("public")
}
